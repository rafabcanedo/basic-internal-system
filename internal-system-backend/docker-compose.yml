services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: internal
    ports:
      - "5432:5432"

  app:
    build: .
    ports:
      - "3333:3333"
    environment:
      - DB_HOST=db
    depends_on:
      - db